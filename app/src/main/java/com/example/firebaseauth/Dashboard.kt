package com.example.firebaseauth

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle

class Dashboard : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_dashboard)


            // if you want to create username and userpassword do you have to call it via the lateinit
        //what is import?
        //some tutorials use private lateinit and declare the type if button
        //but others don't and straight ahead use the finview bu id and declare inside, what difference is that?
    }
}